{
  "name": "splunk-node-webhook",
  "version": "1.0.0",
  "description": "Perform splunk search (optional). Takes the result and converts it to a json format (if not already in json). Post the json file to a given webhook url.",
  "main": "index.js",
  "devDependencies": {
    "bower": "^1.7.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pankaj Sharma",
  "license": "ISC",
  "dependencies": {
    "async": "^2.5.0",
    "config": "^1.26.2",
    "splunk-sdk": "^1.8.2"
  }
}
